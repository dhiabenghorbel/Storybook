import React from "react";
import CircularButton from "./../CircularButton/CircularButton.tsx";

const Home = () => (
  <div>
    <CircularButton
      icon="home"
      value="label.home"
    />
  </div>
);

export default Home;
